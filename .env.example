# Frontend (copy to frontend/.env.local)
NEXT_PUBLIC_API_URL=         # Production: API Gateway URL | Local dev: http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Lambda (set via CDK in production, not needed locally)
TABLE_NAME=                  # DynamoDB table name
AWS_REGION=us-east-1

# Local development (set automatically by dev scripts)
# DYNAMODB_ENDPOINT=http://localhost:8000
# TABLE_NAME=mcrrc-drop-in-runs
