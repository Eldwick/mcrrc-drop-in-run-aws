# Frontend
NEXT_PUBLIC_API_URL=         # API Gateway endpoint URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Lambda (set via CDK, not needed locally unless testing)
TABLE_NAME=                  # DynamoDB table name
AWS_REGION=us-east-1
