version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
            - node ../scripts/resolve-amplify-symlinks.js
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
